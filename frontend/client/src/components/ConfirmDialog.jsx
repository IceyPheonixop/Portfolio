export default function ConfirmDialog({ open, title = 'Delete item?', children, onCancel, onConfirm }) { if (!open) return null; return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4"><div className="panel w-full max-w-md"><h3 className="text-lg font-semibold">{title}</h3><p className="mt-2 text-sm text-zinc-400">{children}</p><div className="mt-6 flex justify-end gap-3"><button className="btn-secondary" onClick={onCancel}>Cancel</button><button className="btn bg-red-500 text-white hover:bg-red-400" onClick={onConfirm}>Delete</button></div></div></div>; }
